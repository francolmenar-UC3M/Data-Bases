SELECT clientId, name, surname, title, season
  FROM ((SELECT title, season FROM SEASON) NATURAL JOIN 
       (SELECT title, season, client FROM lic_series) JOIN 
       clients ON clientId=client)
  

