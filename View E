CREATE VIEW Bacalá AS 
  SELECT title FROM
  (SELECT title, antu
  FROM 
  (SELECT title, COUNT(*) AS antu
    FROM taps_movies 
    WHERE pct BETWEEN 50 AND 97
    GROUP BY title)
   ORDER BY antu DESC)
   WHERE ROWNUM <= 5;
